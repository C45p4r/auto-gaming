# =============================================================================
# Environment Configuration for auto-gaming
# =============================================================================
# Copy this file to .env and adjust values as needed
# 
# This configuration is optimized for Epic Seven automation on Android Studio AVD
# with enhanced OCR, stable window positioning, and reliable ADB input handling.
# 
# Target: Android Studio AVD (Pixel 9a, Android 14, 1080x2424 resolution)
# Input Method: ADB (Android Debug Bridge) - 100% reliable for emulators
# =============================================================================

# Basic Configuration
ENVIRONMENT=dev
LOG_LEVEL=INFO
LOGS_DIR=logs

# OCR (maximize text signal for mobile games)
OCR_LANGUAGE=eng+kor
TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
OCR_PREPROCESS=sharpness
OCR_SCALE=2.0
OCR_PREPROCESS_INTENSITY=2.5
OCR_PSM=7
OCR_OEM=3
OCR_MULTI_PASS=true
OCR_ENSEMBLE=true
OCR_ENGINES=paddle,tesseract_batched,tesseract

# Capture / Window (AVD-optimized positioning and size)
CAPTURE_FPS=2
CAPTURE_BACKEND=window
WINDOW_TITLE_HINT=Pixel_9a|Android|Emulator|AVD
WINDOW_FORCE_FOREGROUND=true
WINDOW_ENFORCE_TOPMOST=true
WINDOW_LEFT=5
WINDOW_TOP=20
WINDOW_CLIENT_WIDTH=1395
WINDOW_CLIENT_HEIGHT=999

# Input (ADB-based - most reliable for Android emulators)
DRY_RUN=false
INPUT_BACKEND=adb
INPUT_BASE_WIDTH=1080
INPUT_BASE_HEIGHT=2424
INPUT_EXCLUDE_BOTTOM_PX=0

# AVD / ADB Configuration
AVD_NAME=Pixel_9a
AVD_RESOLUTION=1080x2424
ADB_TIMEOUT=10
ADB_RETRY_COUNT=3

# Reinforcement learning (bandit)
RL_ENABLED=true
RL_METHOD=bandit
RL_EPS_START=0.35
RL_EPS_END=0.15
RL_PERSIST_PATH=data/policy.json

# Stability and Performance
MAX_CONSEC_ERRORS=5
ERROR_BACKOFF_S=2.0
PARALLEL_MODE=async

# Game-Specific Settings (Epic Seven)
GAME_NAME=Epic Seven
GAME_LANGUAGE=eng+kor
GAME_SAFETY_ENABLED=true
GAME_IAP_BLOCKING=true

# Hugging Face / LLM (optional - for advanced AI features)
HUGGINGFACE_HUB_TOKEN=
HF_MODEL_ID_POLICY=
HF_MODEL_ID_JUDGE=
HF_INFERENCE_ENDPOINT_URL=

# =============================================================================
# AVD Setup Guide
# =============================================================================
# 
# 1. Install Android Studio from https://developer.android.com/studio
# 2. Create AVD: Pixel 9a, API 33/34, 8GB RAM, 32GB storage
# 3. Enable Developer Options: Settings → About Phone → Tap Build Number 7 times
# 4. Enable USB Debugging: Settings → Developer Options → USB Debugging
# 5. Test ADB: adb devices should show "emulator-5554 device"
# 
# Key Configuration Notes:
# - WINDOW_TITLE_HINT: Matches your AVD window title
# - WINDOW_LEFT/TOP: Position your AVD window at these coordinates
# - INPUT_BASE_WIDTH/HEIGHT: Must match AVD resolution (1080x2424)
# - INPUT_BACKEND: Set to "adb" for reliable emulator input
# 
# For Epic Seven:
# - Install Epic Seven from Google Play Store on AVD
# - OCR_LANGUAGE includes Korean (kor) for game text
# - Game safety features prevent IAP and risky actions
# =============================================================================
