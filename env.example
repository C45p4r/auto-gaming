# Copy to .env and adjust values
ENVIRONMENT=dev
LOG_LEVEL=INFO

# OCR
OCR_LANGUAGE=en

# Parallelism
PARALLEL_MODE=async

# Hugging Face / LLM
HUGGINGFACE_HUB_TOKEN=
HF_MODEL_ID_POLICY=
HF_MODEL_ID_JUDGE=
HF_INFERENCE_ENDPOINT_URL=
